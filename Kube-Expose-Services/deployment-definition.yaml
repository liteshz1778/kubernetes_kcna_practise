apiVersion: apps/v1
kind: Deployment
metadata:
    name: myapp-deployment
    labels:
        app: myapp
        type: frontend
spec:
    template:
        metadata:
            name: myapp-pod
            labels:
                app: myapp
                type: frontend
        spec: 
            containers:
                - name: nginx-container
                  image: nginx
                  command: ["/bin/bash"]
                  args: ["-c","apt-get update -y; apt-get install -y iputils-ping"] 
    replicas: 1
    selector:
        matchLabels:
            type: frontend
