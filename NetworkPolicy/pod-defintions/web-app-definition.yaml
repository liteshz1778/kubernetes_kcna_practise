apiVersion: v1
kind: Pod
metadata:
  name: web-app-pod
  labels:
    type: frontend
    app: banking-app
  namespace: qa
spec:
  containers:
    - name: web-app-container
      image: nginx
      ports:
        - containerPort: 1000
